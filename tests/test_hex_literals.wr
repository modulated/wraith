// Test hexadecimal and binary literals

addr TEST_OUTPUT = 0xC000;

fn main() {
    // Hex literals for u8
    a: u8 = 0x40;
    b: u8 = 0xFF;
    c: u8 = 0x00;

    // Hex literals for u16
    x: u16 = 0xFA00;
    y: u16 = 0x1234;
    z: u16 = 0xABCD;

    // Binary literals
    flags: u8 = 0b11010010;

    // Mixed usage
    if a == 0x40 {
        TEST_OUTPUT = 0xFF;
    }
}
