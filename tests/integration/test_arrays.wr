// Test program for fixed-size array support
// Tests array type declarations, initialization, and access

const TEST_OUTPUT: addr = 0xC000;

fn main() {
    // Fixed-size array variable with zero initializer
    buffer: [u8; 5] = [0; 5];

    // Alternative: explicit array literal
    values: [u8; 3] = [1, 2, 3];

    // Test that the declaration was accepted
    TEST_OUTPUT = 0xAA;
}
