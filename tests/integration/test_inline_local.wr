//! Test local inline function

#[inline]
fn my_min(a: u8, b: u8) -> u8 {
    if a < b {
        return a;
    } else {
        return b;
    }
}

const TEST_OUTPUT: addr = 0x6000;

fn main() {
    TEST_OUTPUT = my_min(10, 20);
}
