addr OUT = 0x1000;

fn main() {
    OUT = add(3, 4);
}

fn add(a: u8, b: u8) -> u8 {
    u8 result = 0;
    asm {
        "CLC",
        "LDA {a}",
        "ADC {b}",
        "STA {result}",
    }
    return result;
}