
// Test pointer implementation
#[reset]
fn main() {
    let x: u8 = 42;
    let ptr: *u8 = &x;
    let val: u8 = *ptr;
    
    // Check value
    if val != 42 {
        // Error
        asm { "BRK" }
    }

    // Test pointer arithmetic (if supported, currently maybe not?)
    // Specification said TODO. I won't test arith yet.
    
    // Test u16 pointer
    let y: u16 = 12345;
    let y_ptr: *u16 = &y;
    let y_val: u16 = *y_ptr;
    
    if y_val != 12345 {
        asm { "BRK" }
    }
    loop {

    }
}
